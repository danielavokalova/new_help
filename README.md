# new_help

Demo homepage for New Help Portal (blue hero panel + tiles + quick search).

## Run locally

1. `pip install -r requirements.txt`
2. `streamlit run app.py --server.port 8504`

Then open `http://localhost:8504`.

